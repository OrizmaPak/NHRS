services:
  nginx:
    restart: always
    image: nginx:1.27-alpine
    ports:
      - "80:80"
    volumes:
      - ../../docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../../docker/nginx/conf.d/gateway.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      api-gateway:
        condition: service_healthy
    env_file:
      - ../../.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "nginx -t || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s

  redis:
    restart: always
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api-gateway:
    restart: always
    image: nhrs/api-gateway:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/core-platform/api-gateway/.env
    depends_on:
      auth-api:
        condition: service_healthy
      rbac-service:
        condition: service_healthy
      organization-service:
        condition: service_healthy
      membership-service:
        condition: service_healthy
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  auth-api:
    restart: always
    image: nhrs/auth-api:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/core-platform/authentication/auth-api/.env
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  otp-service:
    restart: always
    image: nhrs/otp-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/core-platform/authentication/otp-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  session-service:
    restart: always
    image: nhrs/session-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/core-platform/authentication/session-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  token-service:
    restart: always
    image: nhrs/token-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/core-platform/authentication/token-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  nin-linking-service:
    restart: always
    image: nhrs/nin-linking-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/core-platform/authentication/nin-linking-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  rbac-service:
    restart: always
    image: nhrs/rbac-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/governance/rbac-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  audit-log-service:
    restart: always
    image: nhrs/audit-log-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/governance/audit-log-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  user-profile-service:
    restart: always
    image: nhrs/user-profile-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/core-platform/user-profile/profile-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  organization-service:
    restart: always
    image: nhrs/organization-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/providers/organization-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  membership-service:
    restart: always
    image: nhrs/membership-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/providers/membership-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  doctor-registry-service:
    restart: always
    image: nhrs/doctor-registry-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/providers/doctor-registry-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  health-records-index-service:
    restart: always
    image: nhrs/health-records-index-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/providers/health-records-index-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  clinical-encounter-service:
    restart: always
    image: nhrs/clinical-encounter-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/providers/clinical-encounter-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  laboratory-result-service:
    restart: always
    image: nhrs/laboratory-result-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/providers/laboratory-result-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  pharmacy-dispense-service:
    restart: always
    image: nhrs/pharmacy-dispense-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/providers/pharmacy-dispense-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  emergency-inventory-service:
    restart: always
    image: nhrs/emergency-inventory-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/emergency/emergency-inventory-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  taskforce-directory-service:
    restart: always
    image: nhrs/taskforce-directory-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/governance/taskforce-directory-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  case-service:
    restart: always
    image: nhrs/case-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/governance/case-service/.env
    depends_on:
      taskforce-directory-service:
        condition: service_healthy
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  ui-theme-service:
    restart: always
    image: nhrs/ui-theme-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/platform-support/ui-theme-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  notification-service:
    restart: always
    image: nhrs/notification-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/platform-support/notification-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

  file-document-service:
    restart: always
    image: nhrs/file-document-service:${NHRS_IMAGE_TAG}
    env_file:
      - ../../.env
      - ../../services/platform-support/file-document-service/.env
    networks:
      - nhrs_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://127.0.0.1:'+process.env.PORT+'/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s

networks:
  nhrs_net:
    driver: bridge

